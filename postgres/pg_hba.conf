# Local Unix socket connections
local   all             all                                     scram-sha-256

# Allow application access only from Docker backend network
host    app_db          app_user         172.30.0.0/16            scram-sha-256
